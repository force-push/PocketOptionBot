# Core
playwright>=1.40.0

# Data & Analysis
pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.4  # probability calibrator (strategy/probability_calibrator.py)
# pandas-ta>=0.3.14b0  # disabled - not compatible with Python 3.14

# Configuration
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Logging & UI
loguru>=0.7.0
rich>=13.0.0

# Telegram (MTProto user session — reads bot DMs)
telethon>=1.34.0

# PocketOption WebSocket API (Rust-backed wheel, supports Python 3.8–3.13)
binaryoptionstoolsv2>=0.2.8

# Dashboard (optional web UI — only needed to run `python3 -m dashboard.server`)
fastapi>=0.110
uvicorn[standard]>=0.29
watchfiles>=0.21

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0